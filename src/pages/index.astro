---
import { getBrowserLanguage } from '../lib/language-detector'

export const prerender = false

const targetLang = getBrowserLanguage(Astro.request)

// Redirect to detected language
return Astro.redirect(`/${targetLang}/`)
---
